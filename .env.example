# ── Cloudflare R2 (catalog export storage) ─────────────────────────
# Public read URL (no auth needed for consumers):
R2_PUBLIC_URL=
# Push credentials (needed for `make push`):
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=pinbase-ingest-sources
